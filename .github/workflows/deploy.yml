name: Deploy to Google Play

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Build & Deploy to Google Play (Internal Track)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Set up JDK 17
        uses: actions/setup-java@c5195efecf7bdfc987ee8bae7a71cb8b11521c00 # v4.7.1
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      # Decode the base64-encoded keystore secret into a file that Gradle can use.
      # To generate KEYSTORE_BASE64, run: base64 -w 0 release.jks
      # (On macOS: base64 -i release.jks | tr -d '\n')
      # Then add the output as a GitHub Actions secret named KEYSTORE_BASE64.
      - name: Decode keystore
        run: |
          printf '%s' "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > "${{ github.workspace }}/app/release.jks"

      # VERSION_CODE combines the run number and run attempt so that both new
      # merges and workflow re-runs always produce a strictly higher code than
      # any previous upload (Play Console rejects duplicate or lower codes).
      # We multiply run_number by 1000 so that even if a workflow is re-run
      # hundreds of times, run_attempt values will not collide with adjacent runs.
      # Note: arithmetic is evaluated in bash because GitHub Actions expressions
      # do not support operators like * or + inside ${{ }}.
      - name: Compute version code
        run: echo "VERSION_CODE=$(( ${{ github.run_number }} * 1000 + ${{ github.run_attempt }} ))" >> "$GITHUB_ENV"

      - name: Build Release AAB
        env:
          KEYSTORE_PATH: ${{ github.workspace }}/app/release.jks
          KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew :app:bundleRelease --no-daemon

      - name: Upload AAB to Google Play (Internal Track)
        uses: r0adkll/upload-google-play@a9f9c9a7769533df15f499a3e9bbf2878f79e7d1 # v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: com.wearamp
          releaseFiles: app/build/outputs/bundle/release/*.aab
          track: internal
          status: completed
