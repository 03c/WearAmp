# Human-readable version name shown in the Play Store and About screens.
# Bump this manually when you ship a significant update (e.g. 1.0.0 -> 1.1.0).
# The versionCode is auto-set to the GitHub Actions run number in CI,
# so you never need to touch that here.
VERSION_NAME=1.0.0
